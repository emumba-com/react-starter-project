// src
import { mergeNewEntities } from '../../../client/utils/index'

describe('client/utils/index', () => {
  describe('mergeNewEntities', () => {
    it('should exist', () => {
      expect(mergeNewEntities).toBeDefined()
    })
  })
})
