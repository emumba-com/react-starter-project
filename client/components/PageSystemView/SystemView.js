// libs
import React from "react"

// src
import styles from "./SystemView.less"

const SystemView = ({ items, environmentSlaAll }) => {
  return (
    <div>
      <div className={styles.root}>
        System View
      </div>
    </div>
  )
}

export default SystemView
